# `gen/mpi_abi/`

Generated (never hand-edited): `entrypoints.c`, the 1376 one-line `MPI_*`/
`PMPI_*` forwarders that make up `libmpi_abi`. NOTES.md §2, §3.

Populated in S2 (the generator core), alongside `gen/include/mpiwrapper_vtable.h`.
