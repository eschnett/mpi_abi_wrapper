# `gen/mpiwrapper/`

Generated (never hand-edited): `wrappers.c` (the per-function conversion
bodies) and `constants.c` (the ABI-to-implementation constant `switch`
tables). NOTES.md §3.

Populated in S2 (generator core, the mechanical argument classes) and S3
(the remaining argument classes: arrays, staging, status arrays, bitmasks,
keyvals, output-string buffers, sentinels, callbacks, `MPI_T`).
