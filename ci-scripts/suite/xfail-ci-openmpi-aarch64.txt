# Expected failures of MPICH's C test suite over this project in GitHub Actions,
# over Open MPI 5.0.10 on **aarch64 only**. This is the delta beside
# xfail-ci-openmpi.txt, which holds what both architectures see; check-tap.py reads
# the two as one and rejects a test listed in both.
#
# One line per test, `<dir>/<program> <np> : why`. A line with no reason after the
# colon fails the run, and so does a listed test that passed.
#
# **Written from run 32182485327**, in which both architectures ran the same three
# shards and disagreed about exactly one test.
#
# **Empty, and it emptied itself.** This file briefly held session/session_4x4 16,
# on the strength of one run in which aarch64 failed it and x86_64 did not. Its own
# note said that one test is a thin basis for an architecture claim and that if it
# moved, this file was the first thing to re-read. Run 32189118611 passed it, so it
# was never an architecture difference -- it is an intermittent test, and the gate
# reported it as "expected failure that passed", which is that mechanism doing
# exactly what it is for.
#
# **And the architecture claim is now measured false rather than merely thin.**
# Run 32861557171 failed it on x86_64 too (job 97846529127, one unexpected failure
# in the leg), 4h21m before run 32887203728 failed it on aarch64. Both
# architectures flap it, so neither delta file is its home: it lives in
# flaky-ci-openmpi.txt, which the two share, and that entry carries the run table.
#
# This paragraph used to end by saying that a test which fails one run and passes
# the next cannot be expressed here at all -- listing it fails the run it passes,
# not listing it fails the run it fails -- and that leaving it unlisted was
# therefore the least bad option, to be read against this note when a run went
# red. That was true when it was written and is not now: --flaky is the third
# category, it gates in neither direction, and it prints which way each run went.
# A flap belongs there.
#
# What still holds is the narrower rule this file exists for: an entry here is a
# claim that the two architectures genuinely differ, and one run is not enough to
# make it.

