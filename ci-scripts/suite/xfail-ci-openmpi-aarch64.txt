# Expected failures of MPICH's C test suite over this project in GitHub Actions,
# over Open MPI 5.0.10 on **aarch64 only**. This is the delta beside
# xfail-ci-openmpi.txt, which holds what both architectures see; check-tap.py reads
# the two as one and rejects a test listed in both.
#
# One line per test, `<dir>/<program> <np> : why`. A line with no reason after the
# colon fails the run, and so does a listed test that passed.
#
# **Written from run 32182485327**, in which both architectures ran the same three
# shards and disagreed about exactly one test.
#
# Nothing here is attributed. One test is a thin basis for an architecture claim,
# so if it moves, this file is the first thing to re-read rather than the last.

session/session_4x4 16 : not yet attributed, and seen on aarch64 only in run 32182485327 -- the other architecture passed it in the same run. Observed: *** An error occurred in MPI_Reduce
